apiVersion: v1
kind: Service
metadata:
  name: todo-chatbot-backend-service
  namespace: todo-chatbot
  labels:
    app: todo-chatbot-backend
spec:
  selector:
    app: todo-chatbot-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      name: http
  type: ClusterIP
